(ns system)

(def homeDir (joker.os/get-env "HOME"))
(def currentUser (joker.os/get-env "USER"))

(defn dir-to-vec [input]
  (as-> input dir
    (joker.os/ls dir)
    (map :name dir)
    (into [] dir)))
